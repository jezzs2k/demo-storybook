import React from 'react';
import Form from '../components/Form';

export default {
  title: 'Form',
  component: Form,
};

export const FormElement = () => <Form />;

FormElement.story = {
  name: 'Form Element',
};
